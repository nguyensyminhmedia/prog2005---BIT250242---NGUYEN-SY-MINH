#include <iostream>
using namespace std;

int main() {
    int n;

    cout << "Nhap so: ";
    cin >> n;

    if (n % 2 == 0) {
        cout << "So chan";
    } else {
        cout << "So le";
    }

    return 0;
}
